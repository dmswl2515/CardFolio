name: Deploy To EC2

on:
  push:
    branches:
        - main

jobs:
    Deploy:
        if: ${{ !contains(github.event.head_commit.message, '[skip ci]') }}
        runs-on: ubuntu-latest

        steps:
            - name: Checkout repository
              uses: actions/checkout@v3
              with:
                token: ${{ secrets.ACCESS_TOKEN }} 

            - name: SSH(원격접속)으로 EC2에 접속하기
              uses: appleboy/ssh-action@v1.2.0
              env:
                APPLICATION_PROPERTIES: ${{ secrets.APPLICATION_PROPERTIES }}
              with:
                host: ${{ secrets.EC2_HOST }}
                username: ${{ secrets.EC2_USERNAME }}
                key: ${{ secrets.EC2_PRIVATE_KEY }}
                timeout: 600s
                command_timeout: 600s
                script_stop: false
                script: |
                    echo "Step 1: Basic test"
                    pwd
                    echo "Step 2: User check"
                    whoami
                    echo "Step 3: Date check"
                    date
                    echo "All steps completed"
                    
                    # Git Repository에서 최신 버전 pull
                    #cd /home/ec2-user/CardFolio

                    # 기존 Gradle 프로세스 정리
                    #pkill -f gradle || true

                    # application.properties 업데이트 
                    #rm -rf src/main/resources/application.properties
                    #git pull origin main
                    #echo "${{ secrets.APPLICATION_PROPERTIES }}" > src/main/resources/application.properties

                    # 메모리 효율을 위한 빌드(테스트 스킵)
                    #./gradlew clean build --no-daemon --max-workers=1 -x test

                    # 톰캣 중지
                    #/home/ec2-user/apache-tomcat-10.1.39/bin/shutdown.sh || true
                    #sleep 5

                    # 기존 애플리케이션 삭제
                    #sudo rm -rf /home/ec2-user/apache-tomcat-10.1.39/webapps/CardFolio*

                    # WAR 파일을 webapps 디렉토리로 복사
                    #echo "Deploying new application..."
                    #cp build/libs/*SNAPSHOT.war /home/ec2-user/apache-tomcat-10.1.39/webapps/CardFolio.war
                    
                    # 권한 설정
                    #chown ec2-user:ec2-user /home/ec2-user/apache-tomcat-10.1.39/webapps/CardFolio.war

                    # 배포된 파일 확인
                    #ls -al /home/ec2-user/apache-tomcat-10.1.39/webapps/

                    # restart tomcat
                    #/home/ec2-user/apache-tomcat-10.1.39/bin/startup.sh

                    # 배포 확인
                    #ls -al /home/ec2-user/apache-tomcat-10.1.39/webapps/CardFolio*
                    #echo "Deployment completed successfully!"
                    


